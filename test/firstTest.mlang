#include <c_std.mlang>

//func shift_args(int* argc, string** argv) -> string {
//    string ret = **argv;
//    *argc -= 1;
//    *argv += 8;
//    return ret;
//}
struct Vec3 {
    int x = 0; 
    int y = 0;
    int z = 0;
    func init() {
        this.x = 0;
        this.y = 0;
        this.z = 0;
    }
    func add(int amount) -> Vec3 {
        this.x += amount;
        this.y += amount;
        this.z += amount;
        return *this;
    }
    func print() {
        c_std::printf("[%d, %d, %d]\n", this.x, this.y, this.z);
    }
}

const ONE = 1;

func main() {
    Vec3* v = c_std::malloc(12);
    c_std::printf("Vec3 typeid = %d\n", Vec3);
    v.init();
    v.print();
}
