#include <c_std.mlang>

func shift_args(int* argc, string** argv) -> string {
    string ret = **argv;
    *argc -= 1;
    *argv += 8;
    return ret;
}

func main(int argc, string* argv) {
    if(argc > 0) {
        c_std::printf("%s\n", shift_args(&argc, &argv));
    }
}
