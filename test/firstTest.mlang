#include <./test/include.mlang>

func twelve() -> int {
    return 12;
}
func main() -> int {
    int x;
    int xx;
    int xy;
    int xz;
    xz = twelve();
    x = 12;
    xx = x;
    x = 14;
    printf("test %d = %d\n", xx, twelve());
    int t;
    t = ten();
    printf("included ten = %d\n", ten());
    return 0;
}
