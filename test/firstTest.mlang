#include <c_std.mlang>

//func shift_args(int* argc, string** argv) -> string {
//    string ret = **argv;
//    *argc -= 1;
//    *argv += 8;
//    return ret;
//}
struct Vec3 {
    int x = 0; 
    int y = 0;
    int z = 0;
    func init() {
        this.x = 0;
        this.y = 0;
        this.z = 0;
    }
    func print() {
        c_std::printf("[%d, %d, %d]\n", this.x, this.y, this.z);
    }
}

const ONE = 1;

func main() {
    Vec3* v = c_std::malloc(12);
    v.init();
    v.x = (ONE+ONE)*32;
    v.y = (ONE+ONE)*32;
    v.z = (ONE+ONE)*32;
    v.print();
}
