#include <c_std.mlang>

func main() {
    {
        int c;
        c = 122; 
        c_std::printf("c = %d\n", c);
    }
    int a;
    a = c_std::system("git --version");
    c_std::printf("hello world, %d\n", a);
}
