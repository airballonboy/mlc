#include <raylib.mlang>

#extern func loop();
func loop() {
    rl::begin_drawing();

    rl::clear_background(rl::get_color(0xffffffff));

    rl::end_drawing();

    loop();
}


func main() {
    rl::init_window(100, 200, "hello");

    loop();
}
