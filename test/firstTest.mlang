#include <std.mlang>
#include <raylib.mlang>


#extern func loop();
func loop() {

    rl::begin_drawing();

    rl::clear_background(65535);

    rl::end_drawing();

    loop();
}

func main() {
    rl::init_window(120, 500, "hello world");
    loop();
    rl::close_window();
}
