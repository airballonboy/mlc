#include <raylib.mlang>


func main() {
    rl::init_window(1200, 675, "hello raylib");
    while (!rl::window_should_close()) {
        rl::begin_drawing();

        rl::clear_background(0xff1515ff);

        rl::end_drawing();
    }
    rl::close_window();
}
