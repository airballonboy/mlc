#include <c_std.mlang>

struct vec3 {
    int x = 0;
    int y = 0;
    int z = 0;
}
func vec_print(vec3 v) {
    c_std::printf("x = %d, y = %d, z = %d\n", v.x, v.y, v.z);
}

func main() {
    vec3 vec;
    vec.x = 13;
    vec_print(vec);
}
