#include <c_std.mlang>

struct CTX {
    int id;
    int args;
    func arg () {
        CTX that = *this;
        c_std::printf("called arg %d\n", that.id);
    }
}

func main() {
    CTX ctx;
    ctx.id = 123;
    ctx.arg();
}
