#include <c_std.mlang>

struct Vec3 {
    int x = 0;
    int y = 0;
    int z = 0;
    func print() {
        c_std::printf("Vec3[%d, %d, %d]\n", this.x, this.y, this.z);
    }
}

func main() {
    Vec3 v;
    v.x = 1;
    v.y = 2;
    v.z = 3;
    v.print();
}
